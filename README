First run "npm install" on command prompt, which will install all needed packages on your local machine.
Second run "npm start" to open the browser, changes will be updated on the browser automatically whenever a file is saved.
